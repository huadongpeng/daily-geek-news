# Python
__pycache__/
*.py[cod]
.venv/
.cache/
tools/.env

# Astro build output (do NOT ignore public/ — it's source assets now)
dist/
node_modules/
.astro/

# Hugo legacy (can be removed once fully migrated)
resources/_gen/

# WeChat article drafts — delivered by artifacts/drafts, not committed
outputs/wechat_articles/
